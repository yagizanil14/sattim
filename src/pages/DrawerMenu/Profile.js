import { SafeAreaView, Text, View } from "react-native";
import React from "react"


const Profile = (props) => {
    return(
        <SafeAreaView>
            <Text>Profile</Text>
        </SafeAreaView>
    )
}

export {Profile}